## Home page banner: the wordmark in Russo One, the tagline and the three entry points.
## Disabled on 2026-09-13 (the home page goes straight to the content); kept here in case
## it comes back. page.vm no longer parses this file and site.scss no longer loads _hero.scss.
#*
<section class="hero">
	<div class="hero-inner">
		<div class="hero-title">STXT</div>
		<p class="hero-tagline">${lang.get("title")}</p>
		<p class="hero-actions">
			<a class="btn btn-primary" href="tutorial">${lang.get("hero.tutorial")}</a>
			<a class="btn" href="https://play.stxt.dev" target="stxt-play">${lang.get("hero.playground")}</a>
			<a class="btn" href="tools">${lang.get("hero.tools")}</a>
		</p>
	</div>
</section>
*#
